#include  "mobilita.h"

int main(){
    int command;
    do{
        scanf("%d",&command);
    }while(command != 1 || command != 2 || command !=3);
    if(command==1){
            scanf("%s[^\n]",);
            start_game();
            readConfigFiles();
    }else if(command ==2){
    
    }else{
     //print   
    }

    while (!isEmpty)
    {
        /* code */
    }
    
}